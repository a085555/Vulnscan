{
  "name": "vulnscan-extension",
  "version": "5.3.0",
  "private": true,
  "scripts": {
    "check": "node --check finding-model.js && node --check request-controller.js && node --check background.js && node --check content.js && node --check dashboard.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  }
}
